# Claude bootstrap

Read and follow the repository-root `AGENTS.md` before acting. `AGENTS.md` is the canonical project instruction source; do not duplicate its policy here. Retrieve deeper repository context only when the current task requires it.
